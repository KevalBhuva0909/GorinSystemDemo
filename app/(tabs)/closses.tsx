import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ClossesScreen = () => {
  return (
    <View>
      <Text>ClossesScreen</Text>
    </View>
  );
};

export default ClossesScreen;

const styles = StyleSheet.create({});
